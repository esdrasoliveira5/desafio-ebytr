export type TokenType = {
  id: string;
  email: string;
};

export type UserEmail = {
  email: string,
};

export type UserOrTaskId = {
  _id: string,
};

export type UserLogin = {
  email: string,
  password: string,
};